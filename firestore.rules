rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow reading and writing of availabilities, shifts, and backup archives
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
